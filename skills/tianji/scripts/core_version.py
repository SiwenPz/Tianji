"""Comparable versions for installer transactions.

Content digests prove identity; only a version can decide which side is newer.
"""

SHARED_CORE_VERSION = "0.2.0"

ADAPTER_VERSIONS = {
    "cmdc": "0.1.0",
}
