# 参与天机 (Contributing)

原型期,欢迎 Issue 和 PR。下面这些事是我们当前最想要人帮忙的,按批次来。

## 第一批征集: 让更多助手能当"总控"

天机的总控 = 用户的主会话,是整个队伍的唯一入口。现在 web 驾驶舱里的总控真会话(流式回复、思维链上屏、工具调用可见)**只有 Claude Code 能做**,因为它有双向 stream-json 协议(见 `tianji/ctrlsession.py`,50 行核心的样子)。

第一批想兼容这五家(它们的 CLI 有结构化协议,最有希望做到和 claude 同级的"真会话"):

| 助手 | 手里的牌 | 要验证/攻克的点 |
|---|---|---|
| **deepseek-harness (dsh)** | ACP/JSON-RPC 服务 + Python SDK,事件流现成 | 协议新、迭代快,接口锁定程度待验 |
| **codex** | `codex app-server`(JSON-RPC 长连接,实验性) | 长连接稳定性;会话续接 |
| **opencode** | `opencode serve`(HTTP/SSE 服务) | 天然 web 友好,最可能最快拿下 |
| **cursor-agent** | `-p --output-format stream-json` 输出流式 JSON | 一次性模式,多轮要 `--resume` 续接 |
| **pi** | 有 RPC/扩展机制 | 协议细节待查证,适合愿意挖文档的人 |

**一个助手的"总控兼容"要做成什么样**(验收标准):

1. web 驾驶舱里能跟它**持续多轮对话**(不是一个命令一 answer 就断)
2. 回复**流式**上屏,别整段蹦
3. 进程死了能如实报告、能重拉
4. 有测试(协议适配层可以拿假进程测,参考 `tests/test_ctrlsession.py`)

想认领哪一家,直接在 Issue 里喊一声。

## 第二批(预告): PTY 托管 + 转录尾随

只有一次性 `-p` 或纯 TUI 的助手(gemini、copilot、kimi、qwen、grok、aider、goose……)没有双向管道,做法是终端托管 + 解析它自己落的会话转录文件。这批等第一批趟平流程再开,有想法也欢迎先聊。

## 接全新助手(工人/审核位)

不是当总控、只是进编制干活的助手,走**新壳接入八问检查单**(规格书 6.7):档 1 钩子/档 2 状态文件/档 3 无头与隔离/provider 绑定方式/协议清单/权限拦截/会话结束事件/思考级别映射,八问答完+三关自测(模拟事件全链路→真会话 spawn→最小任务闭环)就能入库。

## 干活规矩(这几条是硬的)

- **账本(SQLite)是唯一写入口**,任何状态变更走账本 CLI/ops,别旁路
- **核心纯标准库**,加依赖要先在 issue 里说清楚为什么非它不可
- 测试全绿才交:`python -m pytest tests/`
- 文档、注释、对话一律大白话,不堆英文术语
- 机制层的命令/黑话不许甩给用户当使用步骤

## License

贡献即视为以 [MIT](LICENSE) 授权。
