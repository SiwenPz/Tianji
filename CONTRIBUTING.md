# 参与天机 (Contributing)

原型期,欢迎 Issue 和 PR。下面这些事是我们当前最想要人帮忙的,按批次来。

## 第一批征集: 让更多助手能当"总控"

天机的总控 = 用户的主会话,是整个队伍的唯一入口。web 驾驶舱里的总控真会话(流式回复、思维链上屏、工具调用可见)目前有两家能做:**Claude Code**(走它自己的 stream-json 双向协议)和 **kimi**(走 ACP 协议,`kimi acp`)。

兼容代码不按壳写、按协议写: `tianji/ctrlprotocols.py` 里一个协议一个适配层(现有 stream-json、acp 两个),壳只要在自己的条目数据里声明四件事——说哪种协议、用什么命令起、配置目录怎么隔离、key 怎么注入。所以接新壳之前先查一件事: **它说不说这两种协议**。

- 说 ACP 的 → 基本不用写新代码,填一条壳条目数据就行
- 两种都不说、但有自己的结构化协议 → 才需要照着 ctrlprotocols.py 里的样子新写一个协议适配层

第一批想兼容这五家(它们的 CLI 有结构化协议,最有希望做到和 claude 同级的"真会话"):

| 助手 | 手里的牌 | 要验证/攻克的点 |
|---|---|---|
| **deepseek-harness (dsh)** | ACP/JSON-RPC 服务 + Python SDK,事件流现成 | 协议已通(kimi 走的就是 ACP),主要是填条目数据+实测;它迭代快,接口锁定程度待验 |
| **codex** | `codex app-server`(JSON-RPC 长连接,实验性) | 和现有两个协议都对不上,要新写适配层;长连接稳定性、会话续接待验 |
| **opencode** | `opencode serve`(HTTP/SSE 服务) | 要新写适配层,但天然 web 友好,最可能最快拿下 |
| **cursor-agent** | `-p --output-format stream-json` 输出流式 JSON | 一次性模式,多轮要 `--resume` 续接,和 claude 的常驻双向不是一回事 |
| **pi** | 有 RPC/扩展机制 | 协议细节待查证,适合愿意挖文档的人 |

**一个助手的"总控兼容"要做成什么样**(验收标准):

1. web 驾驶舱里能跟它**持续多轮对话**(不是一个命令一 answer 就断)
2. 回复**流式**上屏,别整段蹦
3. 进程死了能如实报告、能重拉
4. 优先复用已有的协议适配层;协议真对不上才新写,别一家一套
5. 有测试(协议适配层拿假进程测,参考 `tests/test_ctrlprotocols.py`)

想认领哪一家,直接在 Issue 里喊一声。

## 第二批(预告): PTY 托管 + 转录尾随

只有一次性 `-p` 或纯 TUI 的助手(gemini、copilot、qwen、grok、aider、goose……)没有双向管道,做法是终端托管 + 解析它自己落的会话转录文件。这批等第一批趟平流程再开,有想法也欢迎先聊。

## 接全新助手(工人/审核位)

不是当总控、只是进编制干活的助手,走**新壳接入八问检查单**(规格书 6.7):档 1 钩子/档 2 状态文件/档 3 无头与隔离/provider 绑定方式/协议清单/权限拦截/会话结束事件/思考级别映射,八问答完+三关自测(模拟事件全链路→真会话 spawn→最小任务闭环)就能入库。

顺嘴说一句: 建档时要是发现这壳有双向流式会话接口,壳条目里有个可选的 ctrl_session 块,顺手填上它就能当总控;没有就不填,不影响入库。

## 干活规矩(这几条是硬的)

- **账本(SQLite)是唯一写入口**,任何状态变更走账本 CLI/ops,别旁路
- **核心纯标准库**,加依赖要先在 issue 里说清楚为什么非它不可
- 测试全绿才交:`python -m pytest tests/`
- 文档、注释、对话一律大白话,不堆英文术语
- 机制层的命令/黑话不许甩给用户当使用步骤

## License

贡献即视为以 [MIT](LICENSE) 授权。
